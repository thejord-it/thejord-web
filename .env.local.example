# Example environment variables
# Copy this file to .env.local and fill in your values

# API URL for client-side requests (must be prefixed with NEXT_PUBLIC_)
NEXT_PUBLIC_API_URL=http://localhost:4000

# API URL for server-side requests
API_URL=http://localhost:4000

# Token for revalidating cache (keep secret in production)
REVALIDATE_TOKEN=dev-token-change-in-production

# Google Analytics 4 Measurement ID
# Get your ID from: Admin → Data Streams → Measurement ID
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-1LDVSZHTTB

# GA4 Measurement Protocol API Secret (for server-side events)
# Create at: GA4 Admin > Data Streams > Measurement Protocol API secrets
GA_API_SECRET=your-api-secret-here
